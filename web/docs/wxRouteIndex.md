# WRouteIndex

One input, four outputs: the value goes out of `out<index>`, the other three branches are skipped (their nodes do not run). See *WRoute* for the mechanism and for how to join the branches again.
